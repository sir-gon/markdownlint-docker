name: Build and Push Docker image

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

env:
  IMAGE_NAME: ${{ vars.DOCKERHUB_REPOSITORY }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build image (local, load)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: ${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }}

      - name: Get markdownlint-cli version
        if: github.ref == 'refs/heads/main'
        id: markdownlint_cli
        run: |
          set -euo pipefail
          # Si la imagen ya tiene `markdownlint-cli` como ENTRYPOINT, pasar --version como argumento.
          OUT=$(docker run --rm ${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }} --version 2>/dev/null || true)
          V=$(echo "$OUT" | grep -oE '[0-9]+(\.[0-9]+){1,}' || true)
          if [ -z "$V" ]; then
            echo "No se pudo detectar la versión de markdownlint-cli. Usando SHORT_SHA como versión fallback."
            V=${{ env.SHORT_SHA }}
          fi
          echo "MARKDOWNLINT_CLI_VERSION=$V" >> "$GITHUB_ENV"
          echo "version=$V" >> "$GITHUB_OUTPUT"

      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag and push image with version
        if: github.ref == 'refs/heads/main'
        run: |
          docker tag ${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }} ${{ env.IMAGE_NAME }}:${{ env.MARKDOWNLINT_CLI_VERSION }}
          docker tag ${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }} ${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.IMAGE_NAME }}:${{ env.MARKDOWNLINT_CLI_VERSION }}
          docker push ${{ env.IMAGE_NAME }}:latest
        env:
          MARKDOWNLINT_CLI_VERSION: ${{ env.MARKDOWNLINT_CLI_VERSION }}

      - name: Publish Docker Hub URL
        id: publish
        if: github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          REPO_URL="https://hub.docker.com/r/${{ env.IMAGE_NAME }}"
          TAG_URL="${REPO_URL}/tags?name=${{ env.PRETTIER_VERSION }}"
          SUMMARY=$(cat <<EOF
          ### Docker Hub
          - Repository: $REPO_URL
          - Tag: $TAG_URL
          EOF
          )
          echo "image_url=$REPO_URL" >> "$GITHUB_OUTPUT"
          echo "tag_url=$TAG_URL" >> "$GITHUB_OUTPUT"
          echo "summary<<EOF" >> "$GITHUB_OUTPUT"
          echo "$SUMMARY" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
          echo "$SUMMARY" >> "$GITHUB_STEP_SUMMARY"